version: '3.9'

services:
  # 1. LiteLLM Proxy - API 服务层 + 语义缓存 + 监控
  litellm-proxy:
    image: ghcr.io/berriai/litellm:main-latest
    build:
      context: ./litellm-proxy
      dockerfile: Dockerfile
    container_name: litellm-proxy
    restart: unless-stopped
    ports:
      - "4000:4000"   # 主 API 端口（内网访问）
    environment:
      #- CONFIG_PATH=/app/config.yaml
      #- PORT=4000
      - LITELLM_LOG=DEBUG
      - DATABASE_URL=postgresql://llmproxy:dbmagedu-com@litellm-db:5432/litellm
      - STORE_MODEL_IN_DB=True
      - LITELLM_MASTER_KEY=magedu.com
      - REDIS_HOST=redis-cache
      - REDIS_PORT=6379
      - REDIS_PASSWORD=magedu.com
    volumes:
      - ./config/litellm-config-redis-semantic-cache.yaml:/app/config.yaml:ro
      - ./logs/litellm:/app/logs
    entrypoint: ["litellm"]                     # 显式保留入口点
    #command: ["--config", "/app/config.yaml", "--port", "4000"]  # 正确：参数数组
    command: ["--config", "/app/config.yaml", "--port", "4000", "--detailed_debug"]  # 正确：参数数组
    healthcheck:
      test: [ "CMD-SHELL", "wget --no-verbose --tries=1 http://localhost:4000/health/liveliness || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - litellm-net

  # 2. litellm-db 
  litellm-db:
    image: postgres:16
    restart: always
    container_name: litellm-db
    environment:
      - POSTGRES_DB=litellm
      - POSTGRES_USER=llmproxy
      - POSTGRES_PASSWORD=dbmagedu-com
    ports:
      - "5432:5432"
    volumes:
      - ./data/postgres:/var/lib/postgresql/data # Persists Postgres data across container restarts
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d litellm -U llmproxy"]
      interval: 1s
      timeout: 5s
      retries: 10
    networks:
      - litellm-net

  # 3. Redis - 双命名空间缓存（语义 + KV）
  redis-cache:
    image: redis/redis-stack:7.4.0-v8
    #image: redis:7-alpine
    container_name: redis-cache
    restart: unless-stopped
    volumes:
      - ./data/redis:/data
      - ./config/redis-stack.conf:/etc/redis-stack.conf:ro
    #command: redis-server /usr/local/etc/redis/redis.conf
    environment:
    - REDIS_ARGS=--requirepass magedu.com --maxmemory 8gb --maxmemory-policy allkeys-lru --save 900 1 --save 300 10 --save 60 10000 --appendonly yes
    networks:
      - litellm-net
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "magedu.com", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

networks:
  litellm-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.21.0.0/24
